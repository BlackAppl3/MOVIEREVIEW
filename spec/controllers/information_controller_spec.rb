require 'rails_helper'

RSpec.describe InformationController, type: :controller do
    
    # before do 
    #     allow(controller).to receive(:authenticate_user!).and_return(false)
    # end

   #  context "Response" do 
       
   #      it "Checking for the response" do
   #         get :index
   #         # expect(response).to redirect_to demos
   #         expect(response).to have_http_status(:success)
   #         #  expect(response).to have_http_status(:forbidden)
   #         # expect(response.body).to include "Hello"
   #      end

   #     it "rendering the response" do    
   #        get :index 
   #        expect(response).to render_template :index
   #     end

   #  end

end
